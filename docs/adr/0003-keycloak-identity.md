# ADR 0003: Keycloak identity

Status: Accepted

Keycloak provides local OIDC without vendor lock-in. Backend JWT validation is enabled by default. DEV_AUTH_ENABLED=true exists only for local smoke and is disabled in .env.example.
