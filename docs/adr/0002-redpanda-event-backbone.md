# ADR 0002: Redpanda event backbone

Status: Accepted

Use Redpanda locally because it is Kafka-compatible and simpler to run in Compose. Production can use Kafka, MSK, Confluent, or Redpanda Cloud while preserving AsyncAPI contracts.
